import styled from "styled-components";

export const Container = styled.div`
    position: fixed;
    left: 92%;
    bottom: 5vh;
    margin-right: 1rem;
    color: var(--light-blue);

    svg{
        cursor: pointer;
    }
`